unit=require('./units');
fs=require('fs');

unit.run('clean', function() {
    var dir = fs.readdirSync('/tmp/test');
    for (var i=0 ; i < dir.length ; i++)
        fs.unlink('/tmp/test/' + dir[i]);
    fs.rmdir('/tmp/test');
});
